{{yield (hash
        header=(component "user-select/header")
        list=(component "user-select/list")
        detail=(component "user-select/detail")
    ) (hash
        users=users
        currentUser=currentUser
        selectUser=(action "selectUser")
        clearUser=(action "clearUser")
    )}}
